var int x_glob = 1;
var int y, z;

var point a = new point; //il faut créer le constructeur

class point {
    attribute int x, y;
    attribute bool b = true;

    method int test (int px) {
        var int l, m = 1, 2;

        return x_glob + px + l + m;
    }
}

main {
    y = a.test(0);
    print(y); //affiche 4
    print(a.b); // affiche true
}